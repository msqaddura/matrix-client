/**
 * Technically this should include all message/event types
 * for time saving will just go with these
 * +hardcoding a value that is only used once makes it used twice
 *
 * Architecture-wise, this should be a git subtree shared between client & server
 */
export const MESSAGE_TYPES = {
    MESSAGE: 'MESSAGE',
    INFO: 'INFO'
};
